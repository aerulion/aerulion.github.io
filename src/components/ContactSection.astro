---
const endpoints = [
    {
        label: 'GitHub',
        value: 'github.com/aerulion',
        note: 'Source, experiments and public work',
        href: 'https://github.com/aerulion'
    },
    {
        label: 'Corpium',
        value: 'corpium.net',
        note: 'The server these systems run on',
        href: 'https://corpium.net/'
    }
];
---

<section class="contact-section" id="endpoints">
    <div class="container">
        <div class="section-header" data-reveal>
            <div class="section-rail">
                <p class="section-eyebrow">Endpoints</p>
                <span class="section-rule" aria-hidden="true"></span>
                <p class="section-count">03 / 03</p>
            </div>
            <h2 class="section-title">Where to find me.</h2>
            <div class="section-lede">
                <p class="section-subtitle">
                    Everything I build publicly lives in one place, and everything I build privately runs in another.
                </p>
                <div class="section-line" aria-hidden="true"></div>
            </div>
        </div>

        <ul class="endpoint-list" data-reveal>
            {endpoints.map((endpoint, index) => (
                    <li class="endpoint">
                        <a href={endpoint.href} class="endpoint-link" target="_blank" rel="noopener noreferrer">
                            <span class="endpoint-index">{String(index + 1).padStart(2, '0')}</span>
                            <span class="endpoint-body">
                                <span class="endpoint-label">{endpoint.label}</span>
                                <span class="endpoint-value">{endpoint.value}</span>
                            </span>
                            <span class="endpoint-note">{endpoint.note}</span>
                            <span class="endpoint-arrow" aria-hidden="true">↗</span>
                        </a>
                    </li>
            ))}
        </ul>
    </div>
</section>

<style>
    .contact-section {
        padding: 0 0 var(--spacing-4xl);
    }

    .endpoint-list {
        list-style: none;
        border-top: var(--hairline) solid var(--white);
    }

    .endpoint {
        border-bottom: var(--hairline) solid var(--white);
    }

    .endpoint-link {
        display: grid;
        grid-template-columns: auto minmax(0, 0.9fr) minmax(0, 1.1fr) auto;
        align-items: center;
        gap: clamp(1rem, 3vw, 3rem);
        padding: clamp(1.4rem, 2.5vw, 2rem) clamp(0.5rem, 1.5vw, 1.25rem);
        text-decoration: none;
        color: inherit;
        transition: background var(--transition-base), padding-left var(--transition-base);
    }

    /* Children inherit colour so the row inverts together; an explicit
       white on any would survive and vanish against the white. */
    .endpoint-link:hover,
    .endpoint-link:focus-visible {
        background: var(--white);
        color: var(--black);
        padding-left: clamp(1rem, 2.5vw, 2rem);
    }

    .endpoint-index {
        font-family: var(--font-family-mono), monospace;
        font-size: var(--label-size);
        letter-spacing: 0.16em;
    }

    .endpoint-body {
        display: grid;
        gap: 0.3rem;
        min-width: 0;
    }

    .endpoint-label {
        font-family: var(--font-family-display), sans-serif;
        font-size: clamp(1.4rem, 2.4vw, 2rem);
        font-weight: 600;
        line-height: 1;
        letter-spacing: 0.04em;
        text-transform: uppercase;
    }

    .endpoint-value {
        font-family: var(--font-family-mono), monospace;
        font-size: var(--label-size);
        letter-spacing: 0.12em;
    }

    .endpoint-note {
        font-size: var(--font-size-sm);
    }

    .endpoint-arrow {
        font-size: var(--font-size-xl);
        transition: transform var(--transition-base);
    }

    .endpoint-link:hover .endpoint-arrow,
    .endpoint-link:focus-visible .endpoint-arrow {
        transform: translate3d(0.25rem, -0.25rem, 0);
    }

    @media (max-width: 768px) {
        .endpoint-link {
            grid-template-columns: auto minmax(0, 1fr) auto;
            gap: var(--spacing-md);
        }

        .endpoint-note {
            grid-column: 2 / -1;
        }
    }
</style>
