---
import Glyph from './Glyph.astro';
import type {GlyphName} from '../data/glyphs';

interface Props {
    href: string;
    variant?: 'outline' | 'solid';
    glyph?: GlyphName;
}

const {href, variant = 'outline', glyph} = Astro.props;
const external = /^https?:/.test(href);
const mark = glyph ?? (external ? 'external' : undefined);
---

<a
    class:list={['btn', variant === 'solid' && 'btn-solid']}
    href={href}
    target={external ? '_blank' : undefined}
    rel={external ? 'noopener noreferrer' : undefined}
>
    <slot />
    {mark && <Glyph name={mark} class="btn-glyph" />}
</a>
