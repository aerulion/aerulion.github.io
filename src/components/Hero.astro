---
import {skills} from '../data/skills';
import LogoMark from './LogoMark.astro';
import LineConstruct from './LineConstruct.astro';
---

<header class="hero-section">
    <div class="container">
        <div class="poster-shell panel-cut">
            <div class="poster-rail poster-rail-top">
                <p class="mono-label">Java / Systems / Plugin architecture</p>
                <div class="poster-ticks" aria-hidden="true">
                    <span></span><span></span><span></span><span></span><span></span><span></span>
                    <span></span><span></span><span></span><span></span><span></span><span></span>
                </div>
                <p class="mono-label rail-end">EU Central / DE / Online</p>
            </div>

            <div class="hero-content">
                <div class="hero-stage">
                    <div class="logo-construct" data-draw>
                        <div class="stage-construct">
                            <LineConstruct/>
                        </div>
                        <div class="stage-mark">
                            <LogoMark drawDelay={520} fillDelay={1500}/>
                        </div>
                        <span class="construct-label construct-label-a">Core / Aerulion</span>
                        <span class="construct-label construct-label-b">Lattice / 30° / 60°</span>
                    </div>
                </div>

                <div class="hero-copy">
                    <p class="hero-kicker mono-label">Professional Java developer / Minecraft system design</p>
                    <h1 class="hero-title">aerulion</h1>
                    <p class="hero-subtitle">
                        Professional Java development for custom Minecraft systems, performance-focused plugins and
                        long-term server architecture.
                    </p>
                    <div class="hero-meta-grid" aria-hidden="true">
                        <span>Java runtime</span>
                        <span>Systems design</span>
                        <span>Gameplay logic</span>
                        <span>Data layer</span>
                    </div>
                    <div class="hero-actions">
                        <a href="#work" class="hero-action hero-action-primary">Selected work</a>
                        <a href="#context" class="hero-action">Context</a>
                    </div>
                </div>
            </div>

            <div class="poster-rail poster-rail-bottom">
                <div class="poster-index" aria-hidden="true">
                    <span>Node / 001</span>
                    <span>Status / Active</span>
                </div>
                <div class="poster-index rail-end" aria-hidden="true">
                    <span>Active since / 2014</span>
                    <span>Selected work / below</span>
                </div>
            </div>

            <ul class="skill-strip" aria-label="Core specialties">
                {skills.map((skill, index) => (
                        <li class="skill-chip">
                            <span class="skill-index">0{index + 1}</span>
                            <span class="skill-name">{skill.name}</span>
                        </li>
                ))}
            </ul>
        </div>
    </div>
</header>

<style>
    .hero-section {
        position: relative;
        z-index: 1;
        padding: clamp(1rem, 2vw, 2rem) 0 var(--spacing-4xl);
    }

    .poster-shell {
        display: flex;
        flex-direction: column;
        min-height: min(calc(100svh - clamp(2rem, 4vw, 4rem)), 980px);
        padding: clamp(1.4rem, 2.4vw, 2.2rem) var(--poster-gutter) var(--poster-gutter);
        position: relative;
        overflow: hidden;
    }

    /* ---- Rails ---- */

    .poster-rail {
        position: relative;
        z-index: 2;
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: var(--spacing-lg);
    }

    .poster-rail-top {
        align-items: center;
        padding-bottom: clamp(1.5rem, 3vw, 2.5rem);
    }

    .poster-rail-bottom {
        padding: clamp(1.25rem, 2.5vw, 2rem) 0 clamp(1rem, 2vw, 1.5rem);
        margin-top: auto;
    }

    .rail-end {
        text-align: right;
    }

    .poster-index {
        display: grid;
        gap: 0.3rem;
        font-family: var(--font-family-mono), monospace;
        font-size: var(--label-size);
        letter-spacing: var(--label-tracking);
        text-transform: uppercase;
    }

    .poster-ticks {
        display: flex;
        align-items: stretch;
        gap: 0.3rem;
        height: 2.4rem;
        flex-shrink: 0;
    }

    .poster-ticks span {
        display: block;
        width: var(--hairline);
        height: 100%;
        background: var(--white);
    }

    .poster-ticks span:nth-child(3n) {
        height: 66%;
        align-self: flex-end;
    }

    .poster-ticks span:nth-child(4n) {
        height: 40%;
        align-self: center;
    }

    /* ---- Main split ---- */

    .hero-content {
        position: relative;
        z-index: 1;
        flex: 1;
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(min(100%, 26rem), 0.92fr);
        gap: clamp(2rem, 5vw, 4.5rem);
        align-items: center;
    }

    .hero-stage {
        min-width: 0;
        display: grid;
        place-items: center;
    }

    /* Square box: scaffolding and mark share one coordinate space, so the
       composition cannot spill past the poster edge. */
    .logo-construct {
        position: relative;
        width: 100%;
        max-width: min(46vh, 520px);
        aspect-ratio: 1;
        display: grid;
        place-items: center;
    }

    /* Wrappers carry this component's scope id; a class handed to a child
       component would not, since Astro scopes styles per component. */
    .stage-construct {
        position: absolute;
        inset: 0;
        display: grid;
        place-items: center;
    }

    .stage-mark {
        position: relative;
        width: 46%;
    }

    .construct-label {
        position: absolute;
        font-family: var(--font-family-mono), monospace;
        font-size: var(--label-size);
        letter-spacing: 0.14em;
        text-transform: uppercase;
        white-space: nowrap;
    }

    .construct-label-a {
        top: -2%;
        right: -2%;
    }

    .construct-label-b {
        bottom: -2%;
        left: -2%;
    }

    /* ---- Copy ---- */

    .hero-copy {
        display: grid;
        gap: var(--spacing-lg);
        align-content: center;
        min-width: 0;
    }

    .hero-title {
        font-family: var(--font-family-accent), sans-serif;
        font-size: clamp(2.75rem, 5vw, 6.5rem);
        font-weight: 700;
        line-height: 0.9;
        letter-spacing: -0.01em;
        max-width: 100%;
    }

    .hero-subtitle {
        font-size: clamp(1rem, 1.3vw, var(--font-size-xl));
        max-width: 42ch;
    }

    .hero-meta-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0.5rem;
        padding-top: var(--spacing-xs);
        border-top: var(--hairline) solid var(--white);
        font-family: var(--font-family-mono), monospace;
        font-size: var(--label-size);
        letter-spacing: 0.14em;
        text-transform: uppercase;
    }

    .hero-meta-grid span {
        padding: 0.5rem 0.6rem;
        border: var(--hairline) solid var(--white);
    }

    .hero-actions {
        display: flex;
        flex-wrap: wrap;
        gap: var(--spacing-sm);
    }

    .hero-action {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-xs);
        padding: 0.85rem 1.4rem;
        border: var(--hairline) solid var(--white);
        text-decoration: none;
        font-family: var(--font-family-mono), monospace;
        font-size: var(--label-size);
        letter-spacing: var(--label-tracking);
        text-transform: uppercase;
        background: var(--black);
        color: var(--white);
        transition: background var(--transition-base), color var(--transition-base);
    }

    .hero-action::after {
        content: '↓';
        font-size: 0.9em;
    }

    .hero-action:hover {
        background: var(--white);
        color: var(--black);
    }

    .hero-action-primary {
        background: var(--white);
        color: var(--black);
    }

    .hero-action-primary:hover {
        background: var(--black);
        color: var(--white);
    }

    /* ---- Skill strip ---- */

    .skill-strip {
        position: relative;
        z-index: 2;
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: var(--spacing-sm);
        list-style: none;
    }

    .skill-chip {
        min-height: 4.5rem;
        display: grid;
        align-content: end;
        gap: 0.35rem;
        padding: 0.85rem 0.95rem;
        border: var(--hairline) solid var(--white);
    }

    .skill-index {
        font-family: var(--font-family-mono), monospace;
        font-size: var(--font-size-xs);
        letter-spacing: 0.14em;
    }

    .skill-name {
        font-size: var(--font-size-sm);
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    @media (max-width: 1100px) {
        .poster-shell {
            min-height: 0;
        }

        .hero-content {
            grid-template-columns: 1fr;
            gap: var(--spacing-2xl);
            padding: var(--spacing-lg) 0;
        }

        .logo-construct {
            max-width: min(62vw, 360px);
        }

        .hero-copy {
            justify-items: start;
        }
    }

    @media (max-width: 768px) {
        .poster-rail-top .mono-label,
        .poster-ticks {
            display: none;
        }

        .poster-rail-top {
            padding-bottom: var(--spacing-md);
        }

        .logo-construct {
            max-width: min(70vw, 290px);
        }

        .construct-label {
            display: none;
        }

        .hero-title {
            font-size: clamp(2.5rem, 13vw, 4.5rem);
        }

        .skill-strip {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .skill-chip {
            min-height: 3.75rem;
        }
    }

    @media (max-width: 560px) {
        .poster-rail-bottom {
            display: none;
        }

        .hero-actions {
            width: 100%;
        }

        .hero-action {
            flex: 1;
            justify-content: center;
        }
    }
</style>
